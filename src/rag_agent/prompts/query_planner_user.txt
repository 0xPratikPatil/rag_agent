User question:
{user_question}

Available sources (ID: description):
{source_catalog}

Produce JSON with:
- selected_sources: a list of source IDs you will query
- queries_by_source: a map from source ID to an array of exactly {queries_per_source} queries

Return JSON only.

